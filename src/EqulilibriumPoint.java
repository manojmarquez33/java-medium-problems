/**
 * Equal Left And Right Sum [ZH]
 *
 * Given an array of integer of size N, print the elements for which the sum of all elements present to it's left is equal to the sum of all elements present to it's right.
 * Boundary Condition:
 * 1<= N <= 10000
 * Input Format:
 * The first line contains N.
 * The second line contains N integers separated by space.
 * Output Format:
 * The first line contains the output as specified.
 * Sample Input/Output 1:
 * Input:
 * 4
 * 2 1 5 3
 * Output:
 * 5
 * Sample Input/Output 2:
 * Input:
 * 6
 * 1 4 -2 2 2 1
 * Output:
 * -2 2
 * */

import com.mano.patterns.*;


import java.util.Scanner;
import java.util.regex.Pattern;

public class EqulilibriumPoint {
    public static void main(String[] args) {

        Scanner in = new Scanner(System.in);
        int n = in.nextInt();
        int a[] = new int[n];
        for(int i=0;i<n;a[i]=in.nextInt(),i++);
        Pattern1 pt = new com.mano.patterns.Pattern1();

    }
}
